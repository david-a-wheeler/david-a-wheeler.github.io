
Garden of Chess Openings

This set of files was created by David A. Wheeler <dwheeler@ida.org>.
It's licensed under the GFDL.


To add a new opening:

* Create a PGN file (use template.pgn to start)
* Create a FEN file (use "xboard -ncp -lgf XX.pgn", File/Save Position)
* Edit "Makefile" to add the gif file to BOARDS, then run "make".

(This requires the "fen2gif" program)

