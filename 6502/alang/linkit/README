
This is the demo linker, which tries to allocate variables to
static locations and overlap them as much as possible.

The linker keeps parameter & local variable allocation separate to
allow calls of the form F(a,b,G(x,y)).  See Crafting a Compiler,
Fischer & LeBlanc, p622.

